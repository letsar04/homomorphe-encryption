version: '3.8'

services:
  cloud-he-server:
    build: .
    container_name: cloud_he_server
    ports:
      - "8000:8000"
    environment:
      - BIND_HOST=0.0.0.0
      - CLOUD_PORT=8000
    volumes:
      - .:/app
    restart: always
